/* The code is exporting an object that contains two plugins: `tailwindcss` and `autoprefixer`. These
plugins are commonly used in web development with JavaScript and CSS. */
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}
